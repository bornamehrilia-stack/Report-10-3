# Report-10-3



---

# 📋 گزارش پروژه: ارتباط سریال دوطرفه با HC 05

## ۱. مقدمه  
.

این پروژه یک **سیستم انتقال داده دوطرفه** است:  
🔹 آردوینو → آندورید (از طریق `Serial`)  
🔹 اندروید → آردوینو (از طریق `Serial`)  


---

## ۳. قطعات مورد استفاده  
- برد آردوینو Uno  
- HC 05 
- برد برد و سیم‌های جامپر  
- کامپیوتر با Serial Monitor
-یک عدد گوشی برای ارسال پیام



## ۵. کد برنامه (اسکچ) 

```cpp
#include <SoftwareSerial.h>

// تعریف پورت سریال مجازی: RX=10, TX=11
SoftwareSerial mySerial(10, 11);

void setup() {
  Serial.begin(9600);        // پورت سریال سخت‌افزاری (برای کامپیوتر)
  
  while (!Serial);          // انتظار برای اتصال Serial Monitor (در بعضی برد‌ها ضروری است)

  mySerial.begin(9600);     // پورت سریال مجازی (برای ماژول خارجی)
}

void loop() {
  // 1. اگر داده‌ای از ماژول خارجی دریافت شده باشد → ارسال به کامپیوتر
  if (mySerial.available()) {
    Serial.write(mySerial.read());  // فوروارد کردن داده به Serial Monitor
  }

  // 2. اگر داده‌ای از کامپیوتر دریافت شده باشد → ارسال به ماژول خارجی
  if (Serial.available()) {
    mySerial.write(Serial.read()); // فوروارد کردن داده به ماژول
  }
}
```


## ۶. نحوه‌ی کارکرد  
1. آپلود کد به آردوینو.  
2. باز کردن Serial Monitor (با سرعت 9600 baud).  
3. اتصال ماژول خارجیHC-05 به پین‌های 10 و 11.  
4. ارسال متن از Serial Monitor → ماژول دریافت می‌کند و از طریق برنامه مربوط به اندروید ،پیام را دریافت میکنیم.  
5. ارسال متن از ماژول → در اندروید خود، متنی را نوشته و ارسال میکنیم و در Serial Monitor نمایش داده می‌شود.  
6. این فرآیند به صورت **زنده و دوطرفه** انجام می‌شود.
.

---
